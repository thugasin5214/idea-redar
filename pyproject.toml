[project]
name = "idea-radar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "praw>=7.7",
    "requests",
    "beautifulsoup4",
    "pyyaml",
    "openai>=1.0",
    "jinja2",
    "python-dotenv",
    "rich",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-mock"]